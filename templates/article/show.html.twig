{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}

<div id="page_article">

    <div class="affichage_contenu_article">

        <div class="titre_contenu_article">
            {{ article.title }}
        </div>

        <div class="contenu_article">
            {{ article.content }}
        </div>

        <div class="date_contenu_article">
            {{ article.date ? article.date|date('Y-m-d') : '' }}
        </div>

    </div>

</div>


    {# <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ article.date ? article.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table> #}

    <a href="{{ path('app_article_index') }}">back to list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
